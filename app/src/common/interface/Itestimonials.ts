export interface review{
	profilePic:string;
	name:string;
	message:string;
	redirect_btn:string; // text on redirect button
	redirect_url:string; // use for redirect user to reviewer Profile (like Facebook Profile)
}